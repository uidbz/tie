# Runtime artifacts generated by the scripts; only the scripts + config are
# tracked. Everything here is recreated by build.sh / start.sh / seed.sh.
/bin/
/db/
/data/
/mnt/
/logs/
/sample-files/
/backups/

# Service configs generated by start.sh (config.toml, the CLI config, is tracked).
/tie-daemon.toml
/tie-filehost.toml
